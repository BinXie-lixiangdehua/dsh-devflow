/**
 * The title as the chip and a floating panel's header show it.
 * @param props - the tab information hook and the bound copy namespace.
 * @returns the DevFlow label for the tab.
 */
export function DevFlowCanvasTitle({ t }) {
    return t('canvas');
}
